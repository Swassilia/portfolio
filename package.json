{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && g-pages -d dist"
  },
  "devDependencies": {
    "terser": "^5.39.0",
    "vite": "^6.2.5"
  },
  "dependencies": {
    "kaboom": "^3000.1.17"
  }
}
